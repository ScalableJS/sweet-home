# ScalableJS — доступ и статус проекта

## Текущий статус
- Работать нужно под пользователем `oleg056@gmail.com`.
- Remote repo: `https://github.com/ScalableJS/sweet-home.git`.
- `master` уже запушена и отслеживает `origin/master`.

## Что это означает
- Remote уже подключён; можно продолжать обычную синхронизацию через `git push` / `git pull`.

## Примечание
- Если появятся дополнительные данные доступа или правила пуша, их стоит дописать сюда же.



